// arithmetic operators = operands (values, variables, etc.)
//                        operators (+ - * /)
//                        ex. 11 = x + 5

/*

    == Operator precedence ==
      1. parathesis ()
      2. exponents 
      3. multiplication & division & modulo
      4. addition & subtraction

      Example: 
        let result = 1 + 2 * 3 + 4 ** 2;

        console.log(result);
             output -> 23 
*/

let students = 31;

// students += 1;
// students -= 1;
// students *= 2;
// students /= 2;
// students **= 2;

// students++; <-- same as: students += 1; OR students = students + 1;
// students--; <-- same as: students -= 1; OR students = students - 1;

console.log(students);